# CHANGELOG

このファイルは、利用者から見た「何がいつ変わったか」を記録するものです。
[Keep a Changelog](https://keepachangelog.com/ja/1.0.0/) の考え方を参考にしています。
まだバージョン番号（v1.0.0など）は採番していないため、日付ごとに記録しています。

各項目の種別は以下の通りです。

- `追加`: 新機能
- `変更`: 既存機能の仕様変更
- `修正`: 不具合修正
- `リファクタリング`: 見た目の動作は変えずに内部のコード構成を整理
- `デザイン`: 機能・計算結果は変えず見た目のみを改善
- `テスト`: テストの追加・改善
- `ドキュメント`: ドキュメントのみの変更

## 2026-07-30

### 変更

- 住居手当の「支給の有無」の入力をプルダウンから2択のラジオボタンに変更
- 勤勉手当の計算方法を修正。「年間支給月数（成績率適用前）」の手入力欄を廃止し、
  勤勉手当 = 算定基礎額 × 成績率のみで算定するようにした（成績率自体が1回あたりの
  支給割合を表すため、月数は別途掛けない）。期末手当の年間支給月数入力は従来どおり

- 住居手当を「支給額の直接入力」から「家賃（月額）の入力＋自動計算」に変更。住居手当額 =
  家賃の半額と28,000円のいずれか低い方（小数点以下切り捨て）とする、実際の計算方法を採用
- 職務の級・号俸の入力を、扶養親族数と同様の「−／＋」ボタン選択式（readonly数値表示）に変更
- 本省手当の「支給の有無」の入力をプルダウンから2択のラジオボタンに変更
- 超過勤務手当の端数処理の順序を、ユーザー提供の計算式に合わせて修正。区分ごとに
  「単価×割増率」を先に四捨五入し、その後に時間数を掛けて合算する方式にした
  （時間単価そのものは四捨五入しない）
- 地域手当の級地区分プルダウンの上に「地域名から選ぶ（任意）」プルダウンを追加。
  代表的な市区町村名（網羅的ではない参考用リスト）を選ぶと級地区分に自動反映される

### 削除

- 行政職俸給表(二)を削除（対象俸給表を20表→19表に変更）
- 本省手当の級別参考額を一覧できる折りたたみ表を削除（金額は自動計算のため不要と判断、
  インラインのヒントテキスト表示は維持）
- 扶養手当欄の「令和8年4月現在の額です」の注記文を削除

## 2026-07-28

### デザイン

- サイト全体の見た目を刷新。CSSカスタムプロパティで色・影・角丸・間隔を一元管理する
  デザインシステムを導入し、計算結果パネルの先頭に年収概算を大きく強調する
  「ヒーロー表示」を追加。フォーム部品・ボタンにホバー/フォーカス時の演出を追加し、
  favicon・meta descriptionも設置した。特定サイトの模倣ではなく、政府系デザイン
  システムや家計・給与計算系サービスに見られる複数の潮流を参考に独自にまとめたもので、
  機能・計算結果に変更はない
- 上記デザイン刷新後にコントラスト比を確認し、年収概算の強調色（グラデーション背景上の
  緑文字）とフォーム部品の枠線の色をWCAG AA基準（通常文字4.5:1、UI部品の境界3:1）を
  満たすよう調整した

### テスト

- 超過勤務手当の月60時間判定について、ちょうど60時間・60.5時間の境界値テストを追加
- administrative_1以外の俸給表を横断するE2Eテストを追加（指定職俸給表＝flat型で級入力欄が
  隠れることの確認、医療職俸給表(一)＝graded型で俸給月額が算出されることの確認）

### リファクタリング

- `js/app.js` と `js/new-hire.js` に重複していたフォーム制御・表示ロジック（俸給表/級/号俸の
  連動、住居形態・通勤手段による表示切替、共通の計算結果表示、俸給表データ出典の注記）を
  `js/form-controls.js` に集約。`js/new-hire.js` は209行→63行に削減。動作に変更はなく、
  E2Eテストに号俸ボタン・表示切替・俸給表バージョン選択・new-hire.htmlの既定値チェックを追加

### 追加

- 本府省業務調整手当（本省手当）を追加。本府省（霞が関）勤務者向けの手当で、住居手当と
  同様に支給がある場合のみ月額を直接入力する方式（既定値0円）とし、月額支給額合計に
  算入。超過勤務手当・期末勤勉手当の算定基礎額には含めない。級別の参考額（要確認）を
  一覧できる折りたたみ表も設置
- 入力内容をこの端末のブラウザ内（localStorage）に自動保存し、次回アクセス時に復元する
  機能を追加。フォーム下部に「保存した入力内容を削除して初期状態に戻す」ボタンを設置し、
  サーバーには送信されない旨を画面上部にも明記
- 地域手当のプルダウンの下に、級地区分（1級地〜5級地・非支給地）ごとの支給割合を
  一覧できる折りたたみ表を追加
- CI（GitHub Actions）を追加。俸給表データの構造検証、計算ロジックのユニットテスト、
  Playwrightによるブラウザでのスモークテストを `main` へのpushとPull Requestのたびに自動実行
- 超過勤務手当（残業代）の計算機能を追加。平日時間外(125%)・深夜(150%)・休日(135%)・
  休日深夜(160%)の割増率と、月60時間超過時の割増率切り替え(150%／175%)に対応
- 公式俸給表データ（行政職・公安職・教育職・医療職など20表）を統合し、俸給表の種類を
  選べるプルダウンを追加
- 号俸に4/6/8号俸ずつ上下できるボタンを追加し、昇給シミュレーションをしやすくした
- フッターに人事院・e-Gov法令検索等の関連機関・法令リンクを追加
- 俸給表バージョン切替機能（現行／人事院勧告後）の基盤を追加。現行データのみ登録済みで、
  人事院勧告後のデータは今後追加予定
- 生涯賃金シミュレーション機能を追加。1〜35年目の級・号俸・地域手当区分を年ごとに設定し、
  生涯賃金合計・年収の平均・最終年の年収を試算できる
- 新規採用職員向けの別ページ（`new-hire.html`）を追加。初任給と、在職期間別割合（期間率）を
  適用した採用初年度の賞与・年収を計算できる

### 変更

- 俸給表データの施行日を令和8年4月1日（2026-04-01）として確定（それまでは暫定的に
  「提供時点（2026-07-28）」として表示していた）
- 号俸の入力をプルダウンから数値入力に変更（級によっては選択肢が100件を超えプルダウンが
  長大になっていたため）
- 扶養手当を令和8年4月現在の額のみに簡素化（年度別の適用年度セレクタを廃止）。配偶者に係る
  手当は廃止済みのため入力欄ごと削除し、子は「15歳以下（13,000円）」「16歳以上22歳以下
  （18,000円、加算5,000円込み）」の2区分に分割した
- 通勤手当を削除（UI・計算ロジック・データから完全に撤去）
- 住居手当を「家賃額を入力すると計算式で算出」する方式から、支給がある場合のみ月額を
  直接入力する方式に変更（既定値は0円）
- 期末手当・勤勉手当を6月期・12月期に分けて表示するよう変更。勤勉手当には新たに
  職員区分（一般職員／特定管理職員（本府省課長等）／指定職職員）と勤務成績区分
  （特に優秀／優秀／良好／良好でない）を選べるプルダウンを追加し、区分に応じた成績率
  （令和8年度、ユーザー提供の人事院公表資料に基づく）を適用。成績率に階級幅がある区分は
  下限値を採用した。期末手当には成績率はかからない
- 号俸の入力をプルダウンに戻した（昇給ボタン4/6/8号俸は維持）
- 扶養親族数（子15歳以下・子16〜22歳・父母等）の入力を数値の直接入力から「−」「＋」
  ボタンによるボタン選択式に変更（0未満にはならない）
- 勤勉手当の職員区分・勤務成績区分を6月期用・12月期用に分離。人事評価は期ごとに行われる
  ため、同一人物でも6月期と12月期で異なる成績区分を設定できるようにした（期末手当は
  引き続き成績率の影響を受けない）
- 本省手当の入力方式を「月額の直接入力」から「支給の有無（支給なし／支給あり）の選択」に
  変更。「支給あり」を選ぶと、左で選択した職務の級から参考額を自動計算するようにした
  （7級以上は一律「7級以上」の額、級のない俸給表を選んでいる場合は0円）
- 住居手当にも「支給の有無」の2択選択を追加。「支給なし（持ち家など）」を選ぶと金額
  入力欄が非表示になり、直前に入力していた金額があっても0円として扱われる
- 本省手当の級別参考額を、ユーザー提供の令和8年度人事院公表資料に基づく値
  （1級9,200円〜7級以上51,800円）に更新。従来のWeb検索ベースの値より確度が上がった
- 超過勤務時間の入力を1時間単位に変更（入力欄の刻み幅を0.5時間→1時間に変更し、
  小数で入力された場合も四捨五入で整数時間に丸める）
- 超過勤務手当の計算方法を変更。従来は全区分の時間外勤務を合算してから最後に
  一括で端数処理していたが、区分（平日通常／平日超過／平日深夜／平日深夜超過／
  休日／休日深夜）ごとに金額を四捨五入してから合算する方式にした

### ドキュメント

- 要件定義書（`docs/requirements.md`）を作成
- プロダクトバックログ（`docs/product-backlog.md`）、Definition of Done / Ready
  （`docs/definition-of-done.md`）、スプリント記録のテンプレートと過去実績
  （`docs/sprint-template.md`、`docs/sprints/`）を作成

## 2026-07-27

### 追加

- 国家公務員給与計算シミュレーターの最初のバージョンを公開。俸給・地域手当・扶養手当・
  住居手当・通勤手当・期末手当勤勉手当（賞与）の入力と、月額給与・年収概算の計算に対応
- 俸給表は行政職俸給表(一)を想定した参考値（推定値）で暫定実装
